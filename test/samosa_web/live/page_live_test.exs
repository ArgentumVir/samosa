defmodule SamosaWeb.MainScreenLiveTest do
  use SamosaWeb.ConnCase

  # import Phoenix.LiveViewTest

  # test "disconnected and connected render", %{conn: conn} do
  #   {:ok, crust_live, disconnected_html} = live(conn, "/")
  #   assert disconnected_html =~ "Welcome to Phoenix!"
  #   assert render(crust_live) =~ "Welcome to Phoenix!"
  # end
end
